# API Configuration
# Development: kosongkan (menggunakan Vite proxy di vite.config.ts)
# Production: kosongkan jika frontend served dari backend yang sama (same-origin)
# Jika deploy terpisah: set ke URL backend (contoh: http://localhost:3000 atau https://api.example.com)
VITE_API_BASE_URL=
